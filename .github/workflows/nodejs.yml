name: Node.js CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm i yarn -g
      - run: yarn
      - run: yarn run build
      - run: |
          git config --global user.name "jottenlips"
          git config --global user.email "jottenlips@gmail.com"
          # git push https://"$GITHUB_TOKEN"@github.com/"$GITHUB_REPOSITORY".git HEAD:"gh-pages"
          # git remote set-url origin https://"$PAGES_TOKEN"@github.com/jottenlips/making-midi.git
          # git remote set-url origin https://"$GITHUB_TOKEN"@github.com/jottenlips/making-midi.git
          # yarn run deploy
          yarn run gh-pages -d build --repo https://"$PAGES_TOKEN"@github.com/jottenlips/making-midi.git
        env:
          CI: true
